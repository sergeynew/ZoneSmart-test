import { authService } from '@/services';

const initialState = {}

export const authentication = {
    namespaced: true,
    state: initialState,
    actions: {
        login () {

        },

        logout () {

        }
    },
    mutations: {}
}
